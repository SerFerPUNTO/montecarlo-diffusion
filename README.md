# Montecarlo-Diffusion

Simulation of diffusive transport using Monte Carlo random walk with parallel execution.

## Structure

- `src/` : source code
- `experiments/` : scripts for running scaling studies
- `results/` : output plots and data

## Requirements

- Python 3.10+
- numpy, matplotlib
- multiprocessing (or mpi4py for optional parallel version)
